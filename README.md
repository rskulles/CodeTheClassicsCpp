# Coding the Classics (in C++)

This repository recreates the games in **Coding the Classics**, but everything is done in C++ instead of Python.


## Building

1. Make sure you have CMake and a Compatible C++ compiler.
2. Easy as ```mkdir build; cd build; cmake ..;cmake --build .;```
```
```
``` 
